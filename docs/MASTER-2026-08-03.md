# AnotherViewer 主文档：第四轮验证 + Wave-1/Wave-2 方案

**日期**: 2026-08-03（整合自 `e2e-round4-2026-08-03.md` / `PLAN-webui-config-expansion.md` / `PLAN-wave2-three-party-model.md`，三件已删除）
**基线**: `93981bf3` / `707255a9`
**用途**: 后续 session 的单一入口——验证结论、发现清单、已批准方案、执行计划。

---

## 0. 红线与协作约定

- **单用户 LAN 架构**：不引入多用户/角色；EH 平台配置仍 Android-local，不进 WebUI
- **设置分区**：可同步用户偏好 → `/settings`（preferences schema）；设备/服务器配置 → `/admin`
- **WebUI 新特性仅 PC/精准定位形态**（鼠标/触控板/手写笔/键盘）：不做移动断点适配；移动端沿用现有 Android 兼容 WebUI，仅回归"不破坏"
- **Android 只测改动面**：未修改的原版功能已知可用，不重复 E2E
- **评审/实现分离**：本模型负责验证、多模态 UI 评审、Markdown 规格；开发模型落地实现
- **configurable-over-hardcoded**：有争议行为做成可配置项（合理默认 + 可切换）
- **neutralize 红线**：不改 `com.hippo.*` 包名/类名；不引入真实画廊域名（`gallery.test`/`showSite*` 态保持）
- **commit 粒度**：每逻辑单元独立 commit，分波提交

### 环境速查（后续 session 直接复用）

```bash
# Java 必须 17+（本机默认 java=1.8）
JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./gradlew :anotherviewer-web:test
JAVA_HOME=/opt/homebrew/opt/openjdk@21 ./build.sh          # 前端先于 bootJar + dist 新鲜度门
node --test mock-server/test-sync.mjs mock-server/test-gallery.mjs
cd web-frontend && npm run typecheck && npm test -- --run

# 隔离实例
ANOTHERVIEWER_DATA_DIR=/tmp/av-x/data nohup $JAVA_HOME/bin/java -jar anotherviewer-web/build/libs/anotherviewer-web-1.1.0.jar --server.port=8083 &
ANOTHERVIEWER_REQUIRE_AUTH=true ... --server.port=8084 &    # auth-on 阶段
AUTHREQ=false bash scripts/api-test.sh http://localhost:8083 auth-off /tmp/av-x/data

# mock 画廊站 + 真机
PORT=4100 node mock-server/server.mjs &
MOCK_API_SIM=1 PORT=4100 node mock-server/server.mjs &   # M-0 门控：/api/v1 同步模拟侧（或 cd mock-server && npm run start:api-sim），默认关
JAVA_HOME=... ./gradlew :app:assembleAppReleaseDebug -PmockEhBaseUrl=http://127.0.0.1:4100
adb reverse tcp:4100 tcp:4100 && adb reverse tcp:8083 tcp:8083
# 平板输入：Gboard 会吞 input text 并关对话框 → 临时 pm disable-user 两个 IME，测完 pm enable 复原
```

---

## 1. 第四轮验证结果（全过）

### 1.1 测试门
web 309 用例 ✅ / mock 23/23 ✅ / 前端 typecheck+450 ✅ / build.sh 新鲜度门 ✅（jar=当前源码已核验）

### 1.2 API 运行时验收（:8083 auth-off / :8084 auth-on）
- `api-test.sh`：auth-off **43/43**；auth-on 鉴权专项 **8/8**（匿名 401 的 29 项属预期）
- 最新修复坐实：`707255a9` partial-update 深合并三案 + auth 坏 body 消息；`93981bf3` null-token push 200 落库兜底空串
- 模块全扫 ✅：配对流（匿名 6 位码→设备 token）、同步域（mode/label/category Int/state 透传、tombstone 增量、since=0、LWW 三段）、收藏/历史 REST（N-5 clamp）、下载状态机（站点不可达优雅 state=4）、快搜 CRUD、备份 export→restore 回环+负例、缓存/指标、站点依赖端点优雅降级、T-3 零 403 噪音

### 1.3 Safari 走查（三主题）
- **T-1 修复生效**（列表行可见）；**E2E-10** 指示器跟随；**E2E-6 Safari 补验通过**（错误态+重试）
- 收藏/历史/设置/管理面板/设备页 ✅；未覆盖：<720 抽屉、阅读器页（工具限制，维持既有结论）

### 1.4 真机 E2E（TB322FC + mock :4100 + :8083）
| 项 | 证据 |
|---|---|
| 配对流（E2E-13） | 服务器出现 `android-…|TB322FC`；token 密文存储 |
| 双向同步 | push：服务器计数上升；pull：app `eh.db` 落入 |
| M-13/M-14 回环 | HISTORY mode=5/3/1；DOWNLOADS label=MyLabel/SmokeLabel |
| E2E-1 | app 同步后 web `/preferences` 200 |
| E2E-12 | 高水位/快照按 baseUrl 分片；新服务器首同步全量 |
| 93981bf3 app 侧 | 本地 gid=0 损坏行被剔除，同步不中断 |

---

## 2. 发现清单（交开发模型）

| ID | 严重度 | 发现 | 定位 | 建议 |
|---|---|---|---|---|
| R4-1 | P2 | listMode 仅首页消费；历史/收藏硬编码网格 | `HomeView.vue:132,251-259`；`HistoryView.vue:313-321`；`FavoriteView.vue:350-356` | ✅ 已消解：共享 GalleryList 三视图消费（a4 p1-wave1/a4-views-b，av-merge 整合） |
| R4-2 | P2 | restore 后运行实例 SQLite 旧句柄全写 500，health 仍 UP | `BackupController.kt:76` | ✅ 已消解（W3 #10）：BackupStateHolder + GET /backup/state + AdminBackup 重启横幅（health 语义不动） |
| R4-3 | P3 | FAB 紫 vs 侧栏 teal = 对齐 Android colorAccent，**非缺陷** | `FabLayout.vue:144`、`tokens.css:26` | 仅记录 |
| R4-4 | P3 | history REST add 的 `mode` 接受不透传 | `GalleryController.kt:48-58`、`HistoryDto.kt:23-31` | ✅ 已接线（W3 #10）：mode 透传持久化 |
| R4-5 | P3 | backup export/restore 未入 openapi | `contracts/openapi.yaml` | ✅ 已补 path（P0 契约冻结） |
| R4-6 | P3 | 空标题收藏显示 Untitled | FavoriteView/GalleryCard | ✅ 已回退 `#gid`（a4） |
| R4-7 | P3 | 收藏场景本地页签计数与 DB 行数不符（0 vs 4），待人工复核 | app FavoritesScene | 实机复核 |
| R4-8 | P3 | mock 未模拟云端收藏 HTML →「解析失败」 | mock-server | ✅ 已消解（W3 #12）：favorites.php 路由 DOM 对齐 + node 用例 + Robolectric FavoritesParserTest 端到端 |

| R4-9 | P3 | WebUI 缩略图被 CSP `img-src 'self'` 拦截，卡片灰占位 | GalleryCard thumb URL=gallery.test | ✅ 已消解（W3 #13+#11）：前端 rewriteSiteAssetUrl 经 image proxy + 端点 fetch-on-miss 回填缓存 |
| R4-10 | P3 | /search 双过滤面共存（legacy SEARCH OPTIONS + FilterPanel） | `SearchView.vue` | ✅ 已收敛（W3 #14）：FilterPanel 单一过滤面 + 高阶 7 位后端化（SearchLayout 冻结保留，彻底移除另立清理） |
| R4-11 | P3 | sort 不入 QuickSearchDto，存预设不含排序 | 契约 QuickSearchDto | ✅ 已入契约（W3 #14，REST-only；app 同步 schema 不含 sort 属预期） |
| R4-12 | P3 | 快删未接服务器（DELETE /quick-search/{id} 存在未用） | `SearchView.vue`/api | ✅ 已接入（W3 #14，离线回退本地+snack） |
| R4-13 | P2 | Tier-2 全表面不可用：服务器无站点协议透传端点，列表/搜索(api.php)/缩略图经拦截器改写后 404（阅读器结构化 API 可用）；fail-closed 保留 | `WebUiBrowseProxyInterceptor`/server | ✅ 已消解（W3 #11）：/api/v1/site/proxy 透传端点 + app Tier-2 改走透传 + Bearer 附载 + mock 让位门 |
| R4-14 | P3 | 服务器 mergeFavorite delete-vs-alive 对同平台无豁免：优先端删除会挡住删端本机后续复活（v2 §3.8 语义已固化） | `SyncService.kt` | ✅ 裁决（2026-08-04 leader）：**保持现语义**——优先端删除即最终意图，删端自复活需经优先端重加；单用户场景下端间分歧罕见，不引入契约变更；服务器矩阵测试已固化该语义 |

**结转项**（见 `docs/TODO-audit-2026-08-02.md` 遗留节）：@Index 落地、M-12 docker 实测、B9 push 全量→增量、H-2/H-4、mock M-0 剥离、<720 抽屉补验、R4-7 人工复核（真机）、真机 Wave-2 项、SearchLayout.vue 彻底移除清理、a5 线 #6 边缘用例加固（p3/a6-v2-a5 备份：honoredTombstone 不再发射/re-add 推活/同平台 LWW 回退细案）。

---

## 3. Wave-1 规格：WebUI 配置扩展（已批准，PC 形态）

### 3.1 搜索高级过滤（1a）
后端 `GET /gallery/search` 扩参（缺省=现状）：`category`(bitmask,已存在仅前端未接) / `sort`→`f_order` / `pageMin|Max`→`f_sp*` / `minRating`→`f_sr*` / `searchName|Tags|Desc|Torrents`→`advsearch=1`+`f_s*`。
- `sort` 语义表（契约权威，mock 为测试 oracle）：0/缺省=默认序，1=发布时间降序，2=评分降序，3=标题升序；服务器透传 `f_order` 到站点 URL
- `GalleryService.search`（`GalleryService.kt:44-60`）改经 `ListUrlBuilder`（解析见 `ListUrlBuilder.java:418-490`），保留 M-5 clamp 与 E2E-6 失败语义；`ListUrlBuilder.java` 补 `setOrder`/`f_order` 解析，归 A1 所有（见 §5.2）
- openapi + mock 识别新参数（`f_cats` 过滤、`f_order` 排序）+ node 用例
前端：SearchBar 搜索态「过滤」按钮 → PC 锚定 popover/固定面板（分类 chips/排序 Select/页数范围/最低评分/搜索范围）；过滤态与 `QuickSearchDto` 同 schema → 「存为快速搜索」复用 `POST /gallery/quick-search`；活动过滤 chip 行；最近搜索 device-local
PC 增强（可选）：卡片 hover 快捷操作、右键菜单、`/` 聚焦搜索、`f` 开过滤

### 3.2 浏览一致性 B 组（1b）
| ID | 规格 |
|---|---|
| B-1 | 共享 `GalleryList` 消费 `listMode`；HomeView 的 localStorage 改 prefs（消解 R4-1） |
| B-2 | `general.showUploader`/`showPostedTime` 默认 false |
| B-3 | `general.defaultFavoriteSlot` 默认 0（clamp -2..9） |
| B-4 | `general.favoriteSlotNames`（`\|` 分隔 10 名，空项回退默认） |
| B-5 | `general.recentSearchMax` 默认 10（0=关） |
schema：`PreferenceDto.kt` GeneralPreferences +5 键（`@field:` 校验）+ `preferences.ts` + `GeneralSettings.vue`；PUT 深合并不变

### 3.3 阅读器深化 A 组（1c，入 `reader` 节可同步）
`backgroundColor`(black/gray/white) / `tapZoneScheme`(threeZone/edgeOnly/disabled) / `keyboardPaging`(true) / `zoomStep`(1.5) / `maxZoom`(5) / `dualPageGap`(8px) / `splitWidePages`(false) / `preloadCount`(2) / `pageTransition`(slide)；`ReaderSettings.vue` 增「交互/双页/性能」分组，控件用 atoms

### 3.4 契约/兼容/分波
openapi 同步新键；app `PreferenceSyncHelper` 忽略未知键（双向不 brick 须验证）；分波 1a→1b→1c，每波三门全绿+独立 commit
**不做**：C/D/E 运维组（押后）、多用户、EH 配置进 WebUI

---

## 4. Wave-2 规格：三方模型与冲突策略（已批准方向，细节可就地优化）

### 4.0 产品模型（用户定义，权威）
1. WebUI 与 App 都可单独连远端平台
2. App 进入 WebUI 网络 → 后台自动同步 + **作为 WebUI 客户端行动**
3. 冲突**默认以 App 为准**，高级面板可选

### 4.1 ADR-0003 决策
- **D1 策略可配**：`device_priority`(默认) | `lww` | `web_priority`；序表 `android > web` 置常量。**不为长期离线做保护** → A/C 下同键冲突**无条件**优先级胜，时间戳仅高水位/展示
- **D2 策略设置 App 权威**：随 `syncPolicy` 下发；WebUI 可改但 App push 覆盖，UI 明示
- **D3 客户端模式分档**：Tier-0 独立 / **Tier-1 同步+流式（默认）** / Tier-2 浏览代理 / Tier-3 下载托管；档位 App 可选；Tier-2/3 路由复用 `MOCK_EH_BASE_URL` 拦截器模式
- **D4 网络感知自动同步**：网络回调 + probe 已配对 baseUrl 分片；`autoSyncIntervalSec` 默认 900（0=仅网络变化）；新网络不自动配对；mDNS 后续可选

### 4.2 选项级详设（§1.5 框架）
**行为矩阵**（开发/测试基线）：
| 分歧场景 | A device_priority | B lww | C web_priority |
|---|---|---|---|
| 同键同时编辑 | App 胜 | 新者胜 | Web 胜 |
| App 删 / Web 保留 | **删除传播** | 保留（删端下轮被复活） | 保留 |
| Web 删 / App 保留 | 保留 | 保留 | **删除传播** |
| 各删不同键·tombstone 实体 | 双向传播 | 双向传播 | 双向传播 |
| 各删不同键·soft 实体 | 双向传播 | **不传播** | 双向传播 |
| 不相交新增 | union | union | union |

> **P0 细化（契约 v2 §3.8）**：A/C soft 实体「双向传播」精确化为——优先端删除无条件传播；非优先端删除在优先端持有该键时不传播（同键优先权优先）、优先端不持有时传播；tombstone 实体（history/bookmark）任何策略下双向传播。B 同 v1（soft 不传播、tombstone 传播）。

**粒度**：Level-1 全局单选项本波；Level-2 按实体组覆盖预留 schema 不做；Level-3 逐条仲裁永不做
**切换语义**：即时生效于后续 merge，**不追溯**重合并（B→A 后需在权威端重做删除；进 tooltip）
**兼容**：旧 App×新服务器收敛于 App 视图 ✓；新 App×旧服务器降级不报错 ✓；B=完整回退兜底 ✓

### 4.3 契约修订（sync-conflict-rules v1.0→v2.0）
§1.2 tie-break 参数化；§3.1/§3.3 union 状态位按策略（A/C 删除可传播）；§3.2/§3.4 A/C 无条件优先（time/page 仅同平台回退）；§4 删除表注策略依赖；§5.1 仲裁总序=策略序→实体专属→收到顺序(仅 B)；新增 §8 syncPolicy（`PUT|GET /api/v1/sync/policy`，pull 附 policy；`sync-schemas.json` 增 `SyncPolicy{conflictStrategy,clientTier,autoSyncIntervalSec}`）

### 4.4 实现要点与分波
server：merge 加 strategy 参 + 单测矩阵（策略×实体×分歧）+ policy 端点；app：同规则 merge、自动同步调度、高级面板（策略/档位/间隔）、Tier 路由；web：`/admin·高级·同步策略` Select + D2 提示
分波 **2a 契约+ADR 冻结 → 2b server → 2c app(Tier1/2) → 2d Tier-3（可押后）**
验收：两策略下真机跑 ①App 删/Web 保留 ②同键同时编辑 ③各删不同 ④网络切换自动同步 ⑤Tier-2 走服务器（access log 佐证）

---

## 5. 后续 session 执行计划（多子代理并行剧本）

> 执行者画像：强模型，善用子代理/并行/工具。本剧本按**能力上限**设计：不相交文件集并发 + worktree 隔离 + 串行缝合点最小化。宁可规则严，不可 scope 糊。

### 5.1 起手（leader，串行）
1. 读本文件 §0 红线、§2 清单、§3/§4 规格；跑 §0 三门确认基线绿
2. 建立 team + task list；按 §5.2 相位创建任务并标注 owned-files
3. 执行 **P0**（见下），冻结契约与所有权矩阵后才放并行波

### 5.2 相位与子代理拓扑

**P0（串行 1 agent，leader 自任亦可）**：2a 契约冻结——`sync-conflict-rules.md` v2、ADR-0003、`sync-schemas.json` SyncPolicy、`openapi.yaml`（sync policy 节 + search 参数 + backup path 补 R4-5）。**所有 contracts/ 文件本相位单一所有权**，并行波只读。

**P1（并行 5 路，worktree 隔离，文件集两两不相交）**：
| Agent | 范围 | owned files（独占） |
|---|---|---|
| A1 server-search（1a 后端） | search 扩参经 ListUrlBuilder、mock 识别 f_cats/f_order + node 用例、web 单测 | `GalleryService.kt` `GalleryController.kt` `anotherviewer-core/.../client/data/ListUrlBuilder.java` `mock-server/*.mjs` `mock-server/test-*.mjs` + 对应测试 |
| A2 server-sync（2b） | merge 参数化（§4.2 矩阵逐案）、`/sync/policy` 端点、policy 持久化、单测矩阵 | `SyncService.kt` `SyncController.kt` `ServerConfigService.kt` + 新 PolicyController + 同步域测试 |
| A3 web-schema-settings（1b/1c schema+设置 UI） | PreferenceDto +6+9 键（含 `general.listMode` grid/list 默认 grid）、preferences.ts、General/ReaderSettings UI | `PreferenceDto.kt` `api/preferences.ts` `views/settings/*` + 其测试 |
| A4 web-views-B（B 组视图） | 共享 GalleryList、History/Favorite 改造、HomeView viewMode→prefs（含 localStorage 迁移）、GalleryCard 信息开关、slot 名 computed | `views/HistoryView.vue` `views/FavoriteView.vue` `views/HomeView.vue` `components/gallery/GalleryList.vue`(新) `GalleryCard.vue` `GalleryGrid.vue` + 其测试 |
| A5 web-search-frontend（1a 前端） | FilterPanel(新)、SearchBar 过滤按钮、存为快速搜索、活动 chip 行；**不碰 HomeView**（接线留 P2） | `components/search/*` + 新 FilterPanel + `views/SearchView.vue` + `api/gallery.ts`（**additive-only**：仅允许新增函数/可选参数，禁改既有签名）+ 其测试 |

**P2（串行 1 agent，缝合）**：HomeView 接线（FilterPanel × viewMode 共存）、ReaderView.vue 阅读器键消费接线（§3.3 九键生效）、前端全量 typecheck/test、openapi 与代码终核。

**P3（并行 2 路，app 树）**：
| Agent | 范围 | owned files |
|---|---|---|
| A6 app-sync-core（2c 核心） | merge 同规则、自动同步调度（D4）、policy 推送（D2） | `webui/WebUiSyncEngine.java` `WebUiSyncStore/Transport*` `SiteDbWebUiSyncStore.java` + 引擎测试 |
| A7 app-ui（2c UI+Tier） | 高级面板（策略/档位/间隔）、Tier-2 浏览代理拦截器 | `webui/WebUiSettings.java` `WebUiConfig.java` `ui/fragment/WebUiSyncFragment.java` + 新拦截器类 |
> 若 A6/A7 在 `EhApplication` 注册点冲突 → A7 降级到 P4 串行。Tier-3（2d）押后独立波。

**P4（验证波，leader + 评审模型）**：三门 + `api-test.sh` 两阶段 + Safari 走查（≥1280 三主题）+ 真机（§1.4 checklist，只测改动面：两策略删除传播/同键同时编辑/各删不同/网络切换自动同步/Tier-2 access log）+ R4-1..8 复验销账。

### 5.3 并行规则（r1–r10，prompt 必带）

- **r1 自包含 prompt**：含本文件相关节原文（§3/§4 语义+矩阵）、owned-files 清单、门命令、done 证据格式；禁止"基于你的研究去修"式委托
- **r2 文件独占**：同相位内每个文件唯一属主；未列文件一律不碰；发现 scope 交叉 → 立即上报 leader，**不得自行扩 scope**，leader 将其降级到下一相位（r8）
- **r3 worktree 隔离**：每 agent `isolation: worktree`；相位结束 leader 按 A1→A5 序合并；冲突由 leader 仲裁，agent 不得互改对方分支
- **r4 commit 策略**：agent 在自己分支内按逻辑单元多 commit（独立可 revert）；相位内**无人**动主分支；leader 每相位一个 merge commit 链
- **r5 门分层**：agent 离场前跑自有范围门（模块 test/typecheck/mock node）；P2/P4 跑全量三门 + api-test；绿 build ≠ done，须附验收实测（§4.2 矩阵逐行、§3 各表）
- **r6 红线注入**：每个 prompt 附 §0 全红线（neutralize/`com.hippo.*`/PC 形态/单用户/configurable）
- **r7 契约只读**：P1/P3 期间 contracts/ 只读；契约疑义记入报告交 leader，回流 P0 式小串行修订
- **r8 并发上限**：同相位 ≤6 路（P1=5 为当前设计上限）；超限或交叉即串行化，不为凑并行拆共享文件
- **r9 验收分离**：agent 不自证完成；最终验收 = leader 跑门 + 评审模型按 §1/§5.2-P4 复跑（含真机）
- **r10 done 证据**：改动文件清单 + 命令输出 + 矩阵逐行实测；缺一项视为未完成

### 5.4 文档与销账
- 本文件为单一主文档；R4-x/结转项消解后在 §2 标 ✅ 并注明 commit；相位结束 leader 在 §5.2 表后追加一行实测结果
- 新发现按 R4-n 续号入 §2；契约级发现走 r7 回流

### 5.5 P1 隔离事故复盘（2026-08-03，leader 记）

- 现象：`isolation: worktree` 对 named teammates 未生效；a5/a4 曾在主目录建分支、a2/a3 曾在主目录提交/留 WIP，形成混合分支 `a4-web-views-b`。
- 处置：leader 预建 worktree（detached at 混合 tip）+ 协议放宽（a2/a4 留主目录、commit 带 [A2]/[A4] 前缀、禁破坏性 git 操作）+ leader 独立 merge worktree（/private/tmp/av-merge）按**路径抽取**合并（owned 路径两两不相交）。
- 规则补充 **r11**：后续相位（P2/P3）一律 leader 预建 worktree 并以 `working_dir` 钉住 agent；agent 不做任何 worktree/branch 级 git 操作，只在被钉目录内编辑/commit。
- 规则补充 **r12**：混合分支上的他人 commit 由 leader 按路径抽取（cherry-pick / checkout -- paths），agent 不得自行 cherry-pick 他人工作。

---

## 6. P4/E2E 执行结果（2026-08-04，leader 实测）

### 6.1 门（av-merge @45cea712）
- JVM：`:anotherviewer-core:test :anotherviewer-web:test :app:testAppReleaseDebugUnitTest` 全绿（web 500+ / app 含策略矩阵 155 例等）
- mock：47/47（A1 oracle 24 + 基线 23）
- 前端：typecheck 0 错 / 573 tests 全过
- `api-test.sh`：auth-off 43/43；auth-on 鉴权专项过（29 匿名 401 为预期基线）

### 6.2 E2E（Playwright Chromium 1440×900，mock 语料经 mock-base-url 拦截器）
- home：history 回退列表 ✅；listMode 切换+持久化 ✅
- 搜索过滤：输入态过滤按钮（home+search）✅；FilterPanel 锚定 ✅；分类 chip 排除/包含语义 ✅（基线≥2 → 排除 Doujinshi=0 → 复原≥2）；活动 chip 行+单删 ✅；存快搜按钮在位 ✅
- 设置：general 六键/reader 九键 UI ✅；admin·高级 同步策略 Select + D2 提示 + PUT 持久化 ✅
- 阅读器：站点 detail 回退后 pages=5 可开 ✅；keyboardPaging ArrowRight 翻页（URL /0→/1）✅；双页渲染+黑底 ✅
- 三主题截图 ✅（dark/light/black 一致）

### 6.3 本轮修复（已提交 av-merge）
- `fix(mock)` filecount 字符串化（EH wire 格式）
- `feat(web)` mock-base-url 拦截器（默认关，E2E/开发用）
- `[P4]` HomeView filter-visible + FilterPanel pointer-events（home 浮层容器 pointer-events:none 继承缺陷）
- `[P4]` GalleryService detail 站点回退（无页历史项可开阅读器，E2E-6 语义保留）
- `test(web)` AdminAdvanced spec 同步策略区断言

### 6.4 编排复盘（r11-r14，追加 §5.3）
- **r11** leader 预建 worktree 并以 working_dir/基线 hash 钉住；agent 自建需报路径
- **r12** 混合分支不改写；集成权威源=av-merge；路径抽取含依赖序（a4 四单元必带 entranceAnimation.spec）
- **r13** task_list 正式任务 + leader 直发为唯一开工依据（sender 字段总线错乱事故后立规）
- **r14** 进程管理：`lsof -ti :PORT -sTCP:LISTEN` 只杀监听者（裸 -i 会连坐后端外连连接）

### 6.5 真机
- 平板未连接（adb 空）：Wave-2 app 侧真机项（两策略删除传播/同键编辑/各删不同/网络切换自动同步/Tier-2 access log）结转，待设备在位补跑；app 单测矩阵已覆盖客户端语义。

### 6.6 结转与 P3 v2 交付（2026-08-04）
- 兜底裁决曾将 #6/#7 结转；后因响应延迟到达，实际全部交付并合入主目录：
  - #7 = a1（p3/a7-v2：Tier-2 拦截器+双注册+面板打磨+16 案，merge 5ecfc3f1）
  - #9 = a1（SpiderQueen 豁免，merge 5b21fd26；p3/spider-exempt 干净备份）
  - #6 = a5（p3/a6-v2-a5：InMemorySyncServer §3.8 对齐 + 客户端矩阵 13 用例/31 行断言，merge cefee72d；app 108/108 绿）
  - a4 并行线 p3/a7-v2-a4 冻结为设计备份（R4-13 参考）；a2 未建分支（启用令未达）。
- worktree 收尾：主目录 BiLi_PC_Gamer 快进为唯一工作副本；P1 五路 worktree 与备份线保留备查。
- 路由错乱事故全程零共享状态破坏；r11–r14 立规固化。

---

## 7. W3 后续清单执行结果（2026-08-04）

| # | 项 | 交付 | 门 |
|---|---|---|---|
| 10 | R4-2+R4-4 | a2：BackupStateHolder+/backup/state+AdminBackup 横幅；history mode 透传（w3/server-fixes，merge 53693c6f） | web 535 / 前端 579→608 整合绿 |
| 11 | R4-13 | a1：/api/v1/site/proxy 透传（白名单/ct/502）+ app Tier-2 改走透传 + Bearer + mock 让位门 + image proxy fetch-on-miss（w3/site-proxy 4 commits，merge 74c493bc/8fdade5c/72525c6f） | web 539 / app 128 |
| 12 | R4-8 | a3：mock favorites.php DOM 对齐 + node 5 例 + Robolectric FavoritesParserTest（w3/mock-favorites，merge fc924450/84fec780） | node 52→55 / app 126 |
| 13 | R4-9 | a4：rewriteSiteAssetUrl + GalleryCard/Detail 经 image proxy（w3/thumb-proxy，merge 607ac230） | 前端 584 |
| 14 | R4-10/11/12 | a5：FilterPanel 单一面 + 高阶 7 位后端化 + 快搜 sort 入契约 + deleteQuickSearch（w3/a5-search-convergence，merge 3c6fc65a；超集裁决全收，含 advanceSearch 0..2047 根因修复） | 前端 591→602 / node 50 / web 绿 |

终验（主目录 @53693c6f）：JVM 三门全绿 / node 55 / 前端 608。R4-2..R4-13 全 ✅；R4-14 裁决保持语义；R4-7/真机项/SearchLayout 清理/a5 边缘用例结转。

---

## 8. W4 + 真机波执行结果（2026-08-04，leader 终裁，主分支 @a3fca766）

### 8.1 整合链（每合并逐门绿；冲突仲裁：InMemorySyncServer 并集逐行核验、WebUiSyncEngineTest 自动合并后三门复验）
| # | 分支 | merge | 交付 | 门亮点 |
|---|---|---|---|---|
| 1 | w4/cleanup | a8255611 | SearchLayout 彻底移除 | 前端 608→587 |
| 2 | w4/sync-edge | 1c62c7f3 | a5 线四资产边缘用例移植（blob 字节级保真） | app 139 |
| 3 | w4/incr-push | f264ac1b | B9 push 增量账本（key→lastModified，损坏→全量回退） | app 146 |
| 4 | w4/index-m0 | 48b44ec3 | @Index 7 实体 19 索引 + M-0 mock 剥离（MOCK_API_SIM 门控） | web 555 / node 54 |
| 5a | w4/deps-sdk | 3e4f34ae | H-2 OkHttp 3.14→4.12 + H-4 targetSdk 34 | JVM 三门 |
| 5b | w4/drawer | eac80fcb | <720 抽屉 Playwright e2e | vitest 587 / drawer 5 |
| fix | w4/notif-fix | 02aa4cee | POST_NOTIFICATIONS 运行时请求（R3-P1）+ F6 裁决注释 | app 146 |
| docs | — | 879fe2e5/1b98773d/af085bbd | 契约 F1 720px 裁决注 / F4 /api/v1/sync 短记法 / §0 M-0 速查 | — |
| P3 | w4/p3-close | ed8eeb52 | C1-C6 收口（H-3 集合断言/drawer 边界/fake skew/SqliteIndexDdlTest/M-0 缺案/R1 补强） | web 556 / node 64 / drawer 8 / api 43/43 |
| W1 | 046a1228 | 01e687fe | R4-15 snapshot 落盘前 7 类键集单点重收 | app 151 + 负向验证 |
| 16/17 | w4/r4-16-17 | 87b50700 | 凭证库失败重试（R4-16 根因）+ favorite/list 过滤墓碑 | app 148 / web 558 |
| fidelity | w4/fidelity | a3fca766 | fake 全 tie-break 镜像 SyncService + tomb-vs-tomb first-received + api-test 复跑稳健 | app 149 / api 43/43×3 |

### 8.2 验证波证据
- leader 亲跑：合并后 JVM 三门绿；node 64/64（含曾漏的 test-favorites）；PRAGMA index_list 19 索引实测（R2-F1 关闭）；build.sh 新鲜度门过
- api-test：auth-off 43/43 + auth-on 8/8（29 匿名 401 预期）；同 data dir 连跑 ×3 43/43（复跑幂等证明）
- V2 Playwright 全回归 16/16（§6.2 七项，连续两轮）；冒烟 6 端点：5×200 + image proxy fetch-on-miss 200（语料路径）/无效路径 404 信封正当
- 真机 TB322FC（**Android 16**）：矩阵 8/8（删除传播 A/lww、同键编辑 A/lww、双向 tombstone、网络切换自动同步、Tier-2 TRACE 佐证）；R4-7 带注关闭（稳态 UI4==DB4，0=预加载缓存标签）；V3：OkHttp4.12+sdk34 无 FATAL/配对保留/同步回环/tier-2 命中

### 8.3 发现裁决（全数本波关闭）
| ID | 裁决 |
|---|---|
| R4-15 | W1 真竞态修复（applyFavorites/applyHistory 不回写 snapshot→detectDeletions 盲区）；W2 同根；W3 经 per-serverKey 全量 push 自愈带注；ack→刷盘窗 P3 带注（可选加固=write-through pending，未立） |
| R4-16 | 根因=WebUiCredentialStore「首败锁死」非 tier 缓存；修复；sim 构建让位门恒关 Tier-2=R4-13 设计带注 |
| R4-17 | FavoriteService 对齐 HistoryService 过滤墓碑 |
| R1-F6 | B9 增量下 lww soft 删除粘性为预期（echo 复活系全量时代偶然收敛）；契约注释+fake 注释修正 |
| R2-F1 | SqliteIndexDdlTest（CI）+ PRAGMA 实测双关闭 |
| R3-P1 | POST_NOTIFICATIONS 运行时请求修复 |
| R4-F1 | 契约 §7 修定 shipped 720px + 漂移裁决注（改实现违移动红线）；640/719/720 边界三用例 |
| R4-F4/F5 | 短记法修正 / §0 速查补行 |
| V1-H3+同族 4 处 | 契约不承诺 pull 顺序→集合断言 |
| M-12 | **裁撤**（用户 2026-08-04 裁决：架构升级层不属本波） |

### 8.4 结转零检查
R5 审计 11/11 三态归位（P1 无主=空）+ 本波新发现全数波内关闭。**带注关闭清单（≠结转，各有裁决理由）**：W3 刷盘窗 / sim 让位门设计 / mock pager meta 25 外观 / R4-7 预加载标签外观 / FGS 6h 平台限制。

### 8.5 编排规则增补（r15-r17）
r15 读写分道（审查 fleet 只读 git refs，不阻写 lane）；r16 写 lane 三件套不相交（工作树∪端口∪硬件）；r17 合并后验证按端口分区（8084-8089/4101 各司其职）。
编排事故存档：V2 prompt env 拼写 ANOOTHERVIEWER（单 O 陷阱已入记忆；V2 自查隔离，产品零影响）；冒烟 `mkdir&&A& B&` 优先级事故自纠。

### 8.6 归档建议
p3/a6-v2-a5 承接使命完成（R5+R1 双验移植超集）可归档；/private/tmp/av-w4-* worktree 可清理。

---

## 9. W5 可用化波（2026-08-04，项目交付即用态）

### 9.1 执行
- A 类关闭：write-through pending 加固（1b2f7776，mutation 验证）/ R4-7 标签真值初始化（40fbbb46，真机佐证无 0→N 跳变）/ mock pager meta 如实化（836c2631，现代 EH wire 形+过滤后真实 total，node 67）
- 生产部署：`./data`（dummy 测试数据）改名 `./data-dev-backup` 可逆备份 → `./start.sh` 常驻 :8080（pid 文件/stop.sh 配套）→ 生产 APK（无 mock 参数）装机 → **无线 LAN 配对** 192.168.6.69:8080 四步验证（connectedDevices 见 TB322FC / app→server 收藏 / server→app 历史 / adb reverse 空、wlan0 直连）
- C 类：worktree 全剪枝；origin push 完成（155 commits；链路不稳以分块+重试克服，见记忆 github-push-flaky-link）
- 平板终态：配对保持、tier=1/interval=900/device_priority、熄屏无限（复原 `settings put system screen_off_timeout 1800000`）

### 9.2 使用入口（交用户）
- WebUI：http://localhost:8080（LAN http://192.168.6.69:8080）；auth 默认关（单用户 LAN 威胁模型），需密码门用 `ANOTHERVIEWER_REQUIRE_AUTH=true` 起
- 启停：`./stop.sh` / `./start.sh`；日志 `anotherviewer-web.log`；数据 `./data`（全新）
- 平板已配对即用；重配对走管理面板 6 位码
- 站点 host 在 app 侧配置（Android-local）；代码 neutralize 保持（gallery.test）

### 9.3 押后项（设计层，非可用性问题，见 §8.3/8.4）
Tier-3 下载托管 / C-D-E 运维组 / Level-2 实体组覆盖 / mDNS / docker 层 / sim 让位门语义 / FGS 6h 平台限制。

### 9.4 W6 UX 修复波（2026-08-05）
走查报告 `docs/UX-review-2026-08-05.md`（18 截图三主题/断点/交互态，0 console error；分类 bit 映射疑云经审视撤销）。全修：
- F-UX1 历史卡叠印（f1f14949）/ F-UX3 FilterPanel 滚动 cue（dce649e9）/ F-UX6 卡片 hover 快捷操作+右键菜单 PC 专属（c469d2c0）——前端 587→616（+29 用例）
- F-UX4 admin 备份导出/导入接入既有 REST+trash 图标、TODO 徽章下线（7670938f）/ F-UX5 tab0=(-1,0) 对齐 app+条目真实 slot 徽章（12240868；slot<0 保持 all 已注释裁决；api-test 43/43×2）
- 生产服务器重建重启生效（dist 新 hash）；双合并自动干净（favorite.ts 增量并集）
